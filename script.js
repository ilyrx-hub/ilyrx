const menu=document.getElementById('menu'),nav=document.getElementById('nav');menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.getElementById('year').textContent=new Date().getFullYear();
const password=document.getElementById('password');document.getElementById('generate').addEventListener('click',()=>{const chars='ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789!@#$%&*';let s='';crypto.getRandomValues(new Uint32Array(18)).forEach(n=>s+=chars[n%chars.length]);password.value=s});document.getElementById('copyPassword').addEventListener('click',async()=>{if(password.value==='Click Generate')return;await navigator.clipboard.writeText(password.value);document.getElementById('copyPassword').textContent='Copied ✓';setTimeout(()=>document.getElementById('copyPassword').textContent='Copy password',1200)});
const counter=document.getElementById('counter');counter.addEventListener('input',()=>{const raw=counter.value,trim=raw.trim();document.getElementById('words').textContent=(trim?trim.split(/\s+/).length:0)+' words';document.getElementById('chars').textContent=raw.length+' characters';document.getElementById('lines').textContent=(raw?raw.split(/\r?\n/).length:0)+' lines'});
const calcInput=document.getElementById('calcInput'),result=document.getElementById('calcResult');function calculate(){const input=calcInput.value.trim();try{if(!input||!/^[0-9+\-*/().%\s]+$/.test(input))throw Error();const value=Function('"use strict";return ('+input+')')();if(!Number.isFinite(value))throw Error();result.textContent='Result: '+value}catch{result.textContent='Result: invalid expression'}}document.getElementById('calcBtn').addEventListener('click',calculate);calcInput.addEventListener('keydown',e=>{if(e.key==='Enter')calculate()});document.querySelectorAll('.calc-pad button').forEach(b=>b.addEventListener('click',()=>{calcInput.value+=b.dataset.v;calcInput.focus()}));
