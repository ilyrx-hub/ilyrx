const menu=document.getElementById("menu"),nav=document.getElementById("nav");menu.addEventListener("click",()=>nav.classList.toggle("open"));document.querySelectorAll("nav a").forEach(a=>a.addEventListener("click",()=>nav.classList.remove("open")));
document.getElementById("year").textContent=new Date().getFullYear();
const search=document.getElementById("search");search.addEventListener("input",()=>{const q=search.value.toLowerCase();document.querySelectorAll(".download").forEach(x=>x.style.display=x.dataset.name.includes(q)?"grid":"none")});
document.getElementById("generate").addEventListener("click",()=>{const chars="ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789!@#$%&*";let s="";for(let i=0;i<18;i++)s+=chars[Math.floor(Math.random()*chars.length)];document.getElementById("password").value=s});
const counter=document.getElementById("counter");counter.addEventListener("input",()=>{const v=counter.value.trim();document.getElementById("words").textContent=(v?v.split(/\s+/).length:0)+" words";document.getElementById("chars").textContent=counter.value.length+" characters"});
document.getElementById("calcBtn").addEventListener("click",()=>{const input=document.getElementById("calcInput").value;try{if(!/^[0-9+\-*/().%\s]+$/.test(input))throw Error();document.getElementById("calcResult").textContent="Result: "+Function('"use strict";return ('+input+')')()}catch{document.getElementById("calcResult").textContent="Result: invalid expression"}});
document.querySelectorAll(".download button").forEach(b=>b.addEventListener("click",()=>alert("Add the official download URL for this item in index.html.")));
